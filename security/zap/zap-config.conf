# OWASP ZAP Configuration for Control Vehiculos API

# Reglas a ignorar (ajustar según necesidad)
# 10021: X-Content-Type-Options Header Missing (ya implementado)
# 10020: X-Frame-Options Header Missing (ya implementado)

# Contexto de la aplicación
# Endpoints públicos (no requieren autenticación)
10021 IGNORE (X-Content-Type-Options)
10020 IGNORE (X-Frame-Options)
10096 IGNORE (Timestamp Disclosure)

# Endpoints que requieren autenticación
# /v1/auth/login - POST
# /v1/auth/refresh - POST

# Endpoints protegidos (requieren JWT)
# /v1/turnos - GET, POST
# /v1/vehiculos - GET
# /v1/evaluaciones - POST

# Configuración de alertas
# IGNORE: Alertas que son falsos positivos
# WARN: Alertas de severidad media
# FAIL: Alertas de severidad alta que deben fallar el build
